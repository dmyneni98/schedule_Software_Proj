//Collatz Correction mainly 

static_global_ void collatz(const long top, int* const maxlen):
    const long i = 3 + (all the thread stuff) * 2;


//Executing timesd code 
collatz <<< ((top -2 )/ 2 + ThreadsPerBlock -1)/ ThreadsPerBlock, ThreadsPerBlock>>>(top,d_maxlen);




//K-means CUDA correction 

const i = (normal thread stuff);



